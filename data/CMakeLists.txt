set(APPLICATION_FILE "performous.desktop")
set(PIXMAP_FILE      "performous.xpm")

install( FILES "${APPLICATION_FILE}" DESTINATION "${SHARE_INSTALL_PREFIX}/applications/")
install( FILES "${PIXMAP_FILE}" DESTINATION "${SHARE_INSTALL_PREFIX}/pixmaps")

