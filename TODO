Features:

- Add analysis
- Add compose screen (using features discussed on mailing list)
- Add configurable microphone latency (to avoid current latency)


Fixes/Enhancement:

- Use cairo_surface_create_for_image instead of SDL_image
